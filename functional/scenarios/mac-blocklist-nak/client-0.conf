# Named mac-blocklist-nak (matching the design doc's v1 scenario list), but
# the correct expected outcome is a timeout, not an explicit NAK:
# `dhcp-host=<mac>,ignore` sets CONFIG_DISABLE, and dispatch_dhcp_with_meta
# returns None for a matching request (src/dhcp.rs) -- no reply is sent at
# all, matching upstream's real "ignore" semantics (a silent drop). See
# tasks.md's P1 entry for this scenario's discovery.
CLIENT_TOOL=busybox-udhcpc
CLIENT_MAC=52:54:00:de:ad:00
EXPECT_RESULT=timeout
